# LaTeX auxiliary files
*.aux
*.log
*.out
*.toc
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.lof
*.lot

# Python
__pycache__/
*.py[cod]
*.pyo
.ipynb_checkpoints/

# macOS
.DS_Store

# Build directory: keep the compiled deliverables, drop the intermediates.
paper/build/*
!paper/build/*.pdf

# Long-running experiment logs and resume checkpoints (regenerable)
logs_*.txt
results/*_checkpoint.json

# Local copy of the v1 sources used for reference while revising.
# The v1 material is already in this repository's history at the root.
manuscript/
